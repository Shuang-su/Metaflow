---
task_id: MF-21-T01
change_id: MF-21
tool: codex
source_task_id: 019fe70e-e5e7-7b81-ae68-39eb0a087128
status: partial
started: 2026-08-09T20:47:00Z
completed: 2026-08-09T21:01:55Z
branch: codex/mf-21-editor-source-ownership
head_before: ecc3b16e52c118ab53a2efd649efe53d3dc91c84
head_after: null
plan_revision: 1
plan_sha256: 77a4fcfd2a42d6cde6a65d7a09835f2fc7b190f3932b940bd059e550e20c5269
archive_status: partial
execution_topology: single-agent
instruction_authority: task-local
authority_source: User-approved Editor source migration, upstream restoration, and dependency PR disposition plan
implementer_id: codex/019fe70e-e5e7-7b81-ae68-39eb0a087128
review_relationship: author-self-review
reviewer_id: codex/019fe70e-e5e7-7b81-ae68-39eb0a087128
control_evidence: null
---

# Agent Completion Record

## Authorized Scope

For MF-21 only: create the T3 MCL pilot records, relocate the current customized Editor source to `metaflow-editor/`, restore the exact official `supersplat-v2.28.0/` snapshot, update ownership metadata and affected build/CI/release/Netlify interfaces, prove behavior and runtime parity, push and open product PR A, obtain its applicable GitHub Gates and Deploy Preview, and mark it Ready without merge. Dependency remediation, production release/deploy, full Viewer E2E, and Issue #15 infrastructure are excluded.

## Execution Method and Authority

- Execution topology: one primary Codex Agent performs the serial migration and external-state operations; no subagent is used.
- Tool-specific instructions and source: GitHub connector for Issue and PR writes, local Git for branch/commit/push, GitHub Actions for hosted checks, and the current user-approved plan.
- Adoption level: task-local MCL pilot.
- Scope of authority: MF-21 paths and Ready endpoint only.
- Review relationship: separate author Spec-compliance and code-quality passes were completed; no independent non-author review is claimed.

## Complete User Request

``````markdown
---
change_id: MF-21
task_id: MF-21-T01
source: codex-user
source_thread_id: 019fe70e-e5e7-7b81-ae68-39eb0a087128
captured: 2026-08-10
message_count: 1
redactions: 0
---

# Complete User Request Transcript

<!-- user-message:1 -->
PLEASE IMPLEMENT THIS PLAN:
# Editor 源码迁移、上游快照恢复与依赖 PR 处置计划

## 1. 执行结果与固定边界

本轮按三个串行 Change 执行：

1. 关闭 PR #4–#8，并归档 Adopt/Skip 决定；
2. 产品 PR A：将 `metaflow-editor` 改为活动源码目录，恢复完整上游 `supersplat-v2.28.0` 快照；
3. 产品 PR B：只修复活动 Viewer/Editor 的相关依赖漏洞。

两个产品 PR 均以“Ready、Preview 和适用 Gate 通过”为终点：

- 不合并产品 PR；
- 不创建正式版本；
- 不执行生产部署；
- 不把 Viewer 完整 E2E 或产品发布验证设为硬门槛；
- 如果 Preview 因既有 Netlify/LFS 问题失败，只报告 `partial`，不得把无关基础设施改造并入产品 PR。

全部修改使用从 `origin/main` 创建的独立 worktree。禁止更新、rebase、reset 或 merge 当前本地 `main`，并禁止触碰未跟踪的 Swiftgram 和 MCL 前身计划。

## 2. PR #4–#8 与 Issue #9

### 2.1 决策矩阵

| PR | 原目标 | 决定 | 后续 |
| --- | --- | --- | --- |
| #4 | 在 `supersplat-viewer-v1.18.2` 升级 `js-yaml` | Skip 并关闭 | 活动 Viewer/Editor 不包含 `js-yaml`，不移植 |
| #5 | 升级 `immutable` | 原 PR Skip 并关闭 | 在产品 PR B 将活动包升级到 `5.1.9` |
| #6 | 升级 `postcss` | 原 PR Skip 并关闭 | 在产品 PR B 将活动包升级到 `8.5.26` |
| #7 | 升级 `concurrently`/`shell-quote` | 原 PR Skip 并关闭 | 在产品 PR B 升级至 `concurrently 9.2.4`、`shell-quote 1.9.0` |
| #8 | 升级 `fast-uri` | 原 PR Skip 并关闭 | 在产品 PR B 将活动锁文件解析到 `3.1.5` |

所有原 PR 均禁止合并，因为其目标是不可变的版本化参考快照。

### 2.2 执行顺序

- 先创建活动依赖安全 Change/Issue，记录目标包、GitHub Advisory、活动路径和不修改参考快照的边界。
- 更新 Issue #9，写入上述逐项决定、依据和后续 Change 链接。
- 分别在 PR #4–#8 留下针对性说明，然后关闭 PR；重新读取并确认：
  - 状态为 `CLOSED`；
  - 没有 merge commit；
  - 快照分支内容未进入 `main`。
- 在 `docs/changes/9-...` 形成 MF-9 Completion Dossier。该治理归档使用独立 PR，在 Gate 通过后可以合并并关闭 Issue #9；它不得包含产品源码或依赖修改。

## 3. 产品 PR A：Editor 源码所有权迁移

### 3.1 目录与接口

创建 T3、明确指定为 MCL 试点的 Editor Source Ownership Change：

- `metaflow-editor/` 成为活动 Editor 源码包；
- `metaflow-editor/dist/` 为构建产物并保持 Git 忽略；
- `supersplat-v2.28.0/` 恢复为不可修改的官方上游快照；
- `/editor/`、导出格式、用户文件兼容和 Editor 版本 `1.1.0` 均保持不变。

活动 Editor 包设置为：

- package name：`metaflow-editor`；
- version：`1.1.0`；
- `private: true`；
- 保留上游 MIT 许可证、来源和本地定制说明。

组件注册表改为：

- `editor` 拥有 `metaflow-editor/**`；
- `reference` 拥有 `supersplat-v2.28.0/**`；
- 路径所有权仅决定检查路由，不授予 Dependabot 修改参考快照的权限。

### 3.2 无损迁移与上游恢复

- 迁移当前定制源码以及全部行为，包括：
  - Metaflow 品牌与版本信息；
  - Legacy ZIP、settings JSON、HTML/SOG package；
  - Service Worker 和 locale；
  - 100000 帧时间轴限制；
  - 现有 Viewer 依赖及导出/重新打开契约。
- 删除当前受版本控制的编译产物，再从活动源码构建 `metaflow-editor/dist`。
- 使用官方 `playcanvas/supersplat` `v2.28.0` 恢复快照，固定：
  - tag object：`ca76baf0c6b7f12a337c1c71a37554eb991a25f9`；
  - commit：`9f4dfe1ff4e94876fb2054353497c8e2eb93b423`；
  - tree：`0ce0d79143abc945e394d1f13f362533a15bf363`；
  - 文件数：232；
  - 规范化树 SHA-256：`9d37961e3ba6259b26f8564e177d5aa4de7d547caa2fd6a8a7ae748c1a6df4a7`。
- 新增机器可校验的 Reference Snapshot 注册信息，CI 根据路径、mode、blob 和规范化摘要验证快照；任何额外、缺失或修改文件均失败。

### 3.3 元数据、构建与部署

- 当前 Editor 元数据改为：
  - `current.sourcePath = "metaflow-editor"`；
  - `current.upstreamSnapshotPath = "supersplat-v2.28.0"`。
- 历史 `1.1` 条目保持原路径和原 Git ref，不伪造其历史；Ledger 追加迁移说明。
- 不新增 Editor Version History 发布条目，因为运行行为和版本没有变化。
- 版本生成器继续维护数据镜像，但运行时文件改为生成到 `metaflow-editor/dist/version.json`。
- Editor CI、Release 和契约测试从 `metaflow-editor` 安装、测试、lint 和构建。
- Netlify 在构建时生成 Editor `dist`，再发布到 Viewer 的 `/editor/`；不再读取仓库中的预编译 Editor。
- PR A 暂不增加 Editor Dependabot 目标，避免在受控依赖修复前自动生成新 PR。

### 3.4 PR A Gate

必须通过：

- 上游快照 commit/tree/file count/digest 完全匹配；
- Editor `npm ci`、测试、lint 和 build；
- Legacy ZIP、settings JSON、HTML/SOG package、帧数限制和品牌/version 契约；
- 构建前后运行时文件清单及内容对比；允许 source map 和预期的版本元数据路径变化，其他差异必须停止；
- 相关治理、元数据、Markdown、Completion 和 `git diff --check`；
- Netlify Deploy Preview 可访问 `/`、`/editor/`、`/editor/version.json` 及 Editor 静态资源。

不要求完整 Viewer E2E、视觉基线、性能采样或生产发布验证。Preview 和适用检查通过后将 PR A 标记 Ready，但不合并，Change 保持 `ready-for-release`。

## 4. 产品 PR B：活动依赖安全修复

PR B 从 PR A Head 创建，并以 PR A 分支为 base，形成可独立审查的 stacked Ready PR。以后只有在 PR A 获准合并后，才允许将 PR B retarget 到 `main` 并重新运行全部 Gate。

仅修改活动包：

- `metaflow-viewer`；
- `metaflow-editor`。

固定升级结果：

- 两个活动包的直接 `postcss` 为 `8.5.26`；
- 两个活动包的直接 `concurrently` 为 `9.2.4`；
- 锁文件中的 `immutable` 为 `5.1.9`；
- 锁文件中的 `fast-uri` 为 `3.1.5`；
- 锁文件中的 `shell-quote` 为 `1.9.0`；
- 不增加或升级 `js-yaml`；
- 不修改任何 `supersplat-v*` 或 `supersplat-viewer-v*` 快照。

在此 PR 中才将 `/metaflow-editor` 加入 Dependabot 的活动 npm 目录。

PR B 必须验证：

- 锁文件精确解析为上述版本；
- 对应 GitHub Advisory 已不再适用于活动依赖树；
- 未引入新的直接依赖或意外 major 升级；
- Viewer 执行 `npm ci`、test、typecheck、build；
- Editor 执行 `npm ci`、test、lint、build；
- 构建产物如出现非预期运行时代码变化，必须停止并调查；
- Preview 对 `/` 和 `/editor/` 执行基础 smoke；
- 不把无关的既有 audit 警告伪报为本 Change 已解决。

通过适用 Gate 后将 stacked PR 标记 Ready，但不得合并、retarget、发布或部署到生产。

## 5. 完成记录与停止条件

每个独立任务必须保存完整用户问题、获准计划、行动与回复摘要、验证、副作用和真实状态。

最终状态应分别为：

- MF-9：PR #4–#8 已关闭、决策已归档后可以 `closed`；
- Editor 迁移 Change：PR A Ready，但未合并，状态 `ready-for-release`；
- 依赖安全 Change：PR B stacked Ready，等待 PR A，状态 `verifying` 或 `ready-for-release`，不得 `closed`。

发生以下任一情况立即停止并报告 `partial`：

- 上游快照无法精确重建；
- 当前 Editor 定制行为无法无损迁移；
- 运行时产物出现未获准差异；
- 目标依赖仍落入已知受影响范围；
- required check 或 Preview 失败；
- 为通过 Preview 必须扩大到 Issue #15 的 LFS/Netlify 基础设施改造；
- GitHub 状态无法事后重读；
- 当前本地 `main`、未跟踪 Swiftgram 或 MCL 前身计划发生变化。
<!-- /user-message -->
``````

## Complete Effective Task Plan

``````markdown
---
change_id: MF-21
title: Migrate Editor source ownership and restore upstream v2.28.0
status: planned
component:
  - editor
  - reference
  - platform
risk: T3
type: architecture
owner: Shuang-su
created: 2026-08-10
updated: 2026-08-10
issue: https://github.com/Shuang-su/Metaflow/issues/21
plan_revision: 1
completion_state: pending
supersedes: null
terminal_reason: null
---

# Implementation Plan

## Goal

Deliver a Ready, unmerged PR that makes `metaflow-editor/` the Active Editor 1.1.0 source, restores `supersplat-v2.28.0/` to the exact official upstream snapshot, and proves that user-visible behavior and runtime output are unchanged.

## Architecture summary

The Active fork, its ignored `dist/`, metadata generator, CI, release workflow, and Netlify build form the product path. A separate reference registry and validator own the immutable upstream snapshot. Version History remains the release fact source; MF-21 only records source relocation in current metadata and the ledger.

## Preconditions and dependencies

- Work only in `/Volumes/Prism/Metaflow-editor-migration` on `codex/mf-21-editor-source-ownership`, based on `origin/main` at `ecc3b16e52c118ab53a2efd649efe53d3dc91c84`.
- Preserve `/Volumes/Prism/Metaflow` local `main` at `47ffd86e9635d7d797628a214df817f7ca60b55c`, its nine local commits, the untracked Swiftgram directory, and the predecessor-plan checksum.
- Use Node 20.19.0 for package commands.
- Fix upstream identity to tag object `ca76baf0c6b7f12a337c1c71a37554eb991a25f9`, commit `9f4dfe1ff4e94876fb2054353497c8e2eb93b423`, tree `0ce0d79143abc945e394d1f13f362533a15bf363`, 232 files, and canonical digest `9d37961e3ba6259b26f8564e177d5aa4de7d547caa2fd6a8a7ae748c1a6df4a7`.
- The verified baseline build must remain byte-identical to the committed runtime except source maps and generated `version.json`.

## Branch and worktree strategy

Make all tracked changes in the isolated worktree. Commit and push only the MF-21 branch. Open PR A against `main`, keep it Draft until the exact Head passes applicable GitHub Gates and Netlify Preview, then mark it Ready without merge.

## Tasks

### MF-21-T01 — Source migration, snapshot restoration, validation, and Ready PR

- Authorized scope: MF-21 Proposal, Spec, Plan, Active source relocation, exact snapshot restoration, ownership metadata, tests, CI/release/Netlify routing, Evidence, Completion, push, PR, Preview, and Ready transition.
- Target paths: `metaflow-editor/**`, `supersplat-v2.28.0/**`, Editor/reference metadata, relevant scripts/tests/workflows, `netlify.toml`, repository documentation, and `docs/changes/21-editor-source-ownership/**`.
- RED boundary: current ownership routes Active Editor work through a versioned path; current reference validation lacks v2.28.0 identity; current version generator and Netlify consume committed build output.
- GREEN implementation: move the customized source to the Active path, restore the official archive, register and validate its canonical identity, update product pipelines, and add behavior and artifact contracts.
- REFACTOR boundary: only path/ownership and supporting validation cleanup; no feature, dependency, visual, export, data-format, or version change.
- Validation: exact snapshot identity, Node 20.19.0 `npm ci/test/lint/build`, behavior contracts, pre/post runtime comparison, metadata and generator checks, MCL/platform/Markdown/Completion/diff checks, GitHub required Gate, and Deploy Preview route/static-asset smoke.
- Commit and PR boundary: one or more reviewable MF-21 commits and one Ready, unmerged PR A against `main`.
- Serial dependency: finish MF-21 and make PR A Ready before creating MF-2 from its exact Head.
- Rollback and stop: stop `partial`, keep PR Draft, and do not merge when snapshot identity, behavior, artifact parity, applicable hosted Gate, or Preview fails, or when success requires Issue #15 scope.

## Final validation

- `metaflow-editor` has package name `metaflow-editor`, version `1.1.0`, `private: true`, and ignored `dist/`.
- The snapshot registry validator proves all fixed upstream identifiers and the canonical tracked-tree digest.
- No historical Editor Version History entry is rewritten and no release entry is added.
- No dependency target version changes occur and Dependabot still excludes `/metaflow-editor` until MF-2.
- The baseline and migrated runtime file sets/content match under the approved exclusions.
- PR A changes no Viewer product source and passes applicable hosted checks and Preview smoke.
- Original local `main` and untracked materials remain unchanged.

## Release, observation, and closure

There is no merge, release, production deploy, or production observation in MF-21-T01. When the Ready endpoint is proven, set the Change to `ready-for-release`, archive the Task as complete, and leave the Change open pending future human merge authorization. If any mandatory Ready Gate is not proven, record `partial` and the exact continuation condition.
``````

## Chronological Action Summary

| # | Action | Read / write | Target | Outcome and evidence |
| ---: | --- | --- | --- | --- |
| 1 | Protected and inspected the original workspace and isolated worktree | read/write ref | Git/local paths | Local main safety ref created; work isolated from user files. |
| 2 | Verified current source and official upstream identity | read | Editor source/upstream repository | Baseline source delta and exact upstream tag/commit/tree/file count/digest established. |
| 3 | Rebuilt and compared current Editor runtime | read/ignored build | current source and runtime | Node 20.19.0 lint/build passed; approved-exclusion comparison found no differences. |
| 4 | Created MF-21 Proposal, Spec, Plan, request snapshot, and draft Completion | write | `docs/changes/21-editor-source-ownership` | Gate 3 inputs captured before product-source mutation. |
| 5 | Rebased the uncommitted isolated worktree onto the latest remote governance/routing baseline | read/write | MF-21 worktree only | Integrated `origin/main@b9c54c0c`; resolved CI routing and component-registry overlap without touching local `main`. |
| 6 | Moved the customized Editor source to the Active package and restored the versioned directory from official upstream | write | `metaflow-editor/**`, `supersplat-v2.28.0/**` | Active source preserved; snapshot now reproduces the fixed 232-file upstream tree and digest. |
| 7 | Added fail-closed source, runtime, metadata, and path-ownership contracts | write | metadata, scripts, tests, CI/release/Netlify | Activity and reference paths route independently; generated artifacts no longer live in Git. |
| 8 | Ran local product and governance Gates under Node 20.19.0 | read/ignored build | Editor, Viewer, repository validators | Editor 4 tests/lint/build and 26-file parity passed; Viewer 52 tests/type/build passed without E2E; 60 Node and 10 Python repository tests passed. |
| 9 | Performed separate Spec-compliance and code-quality author review passes | read | complete staged diff | Scope and behavior matched the Spec; validators fail closed and no independent-review claim was made. |
| 10 | Rebased the committed implementation onto the latest remote documentation baseline | read/write | MF-21 branch only | Replayed cleanly on `origin/main@ecc3b16e`; user local `main` remained untouched; affected checks were rerun before the force-with-lease update. |
| 11 | Pushed the branch and opened Draft PR #25 | external write/read-back | GitHub | Re-read Draft/open, base `main`, exact Head `0b64e35a`, and mergeable state before monitoring hosted checks. |
| 12 | Inspected the first hosted failures and applied the approved in-scope CI-input correction | read/write | PR #25 logs, workflow sparse inputs, README contract test | Kept product bytes unchanged; added missing attributes/snapshot/registry inputs and updated the stale PR #24 README digest baseline. |

## Agent Reply Summary

The Agent reported that the Active Editor migration and upstream restoration are locally verified, including exact runtime and snapshot identities. The task remains `partial` until the pushed PR Head passes GitHub Gates and Deploy Preview and can be marked Ready without merge.

## Files and External Effects

- Created Issue #21 and re-read its accepted T3 pilot scope.
- Created the isolated branch/worktree and a local safety ref for the original main.
- Moved the complete customized source into `metaflow-editor/`, removed tracked generated runtime files, and restored `supersplat-v2.28.0/` from the official tag.
- Added the reference registry/validator, runtime baseline/validator, Editor contract tests, metadata generator checks, CI routing, and build-source updates.
- Generated only ignored `node_modules/` and `dist/` outputs during validation; no release, production deploy, PR merge, or local-main update occurred.
- Created recoverable temporary backups under `/tmp/mf21-editor-swap.fCg3Uv`; moved two untracked `.DS_Store` files there after validation detected them as extras.
- Pushed `codex/mf-21-editor-source-ownership` and created Draft PR #25 against `main`; no Ready or merge transition has occurred yet.

## Validation

- Exact upstream identity: passed at tag object `ca76baf0...`, commit `9f4dfe1f...`, tree `0ce0d791...`, 232 files, canonical SHA-256 `9d37961e...`.
- Editor: `npm ci`, 4 contract tests, source lint, build, version generation/check, and 26-file runtime comparison passed.
- Viewer: `npm ci`, 52 tests, typecheck, and build passed; E2E was not selected or run.
- Repository: 60 Node tests and 10 Python tests passed; MCL strict, registry, routing, Version History, platform, data, Markdown, secret/accidental-file, YAML, and staged diff checks passed.
- Source move review: pre-migration customized tree and Active source differ only in approved package identity, README, tests, and `.gitattributes`; lock dependency graph is unchanged.

## Failures, Retries, and Skipped Checks

- Expanding lint to the new test file failed because inherited `eslint-plugin-import` does not support the repository's ESLint 10 API; the approved existing `eslint src` boundary was restored, while `node --test` validates the test itself.
- The first exact snapshot check failed on one `.DS_Store`; it was moved to a recoverable temporary directory and the 232-file identity then passed.
- A Ruby 2.6 YAML parsing option was unsupported; rerunning with the compatible parser API succeeded for both workflows.
- Editor install still reports 7 high and 2 critical audit findings, and Viewer reports 1 moderate and 4 high; these are assigned to MF-2 and are not claimed fixed here.
- PR #25 Head `0b64e35a` failed docs/governance/Viewer/Editor because sparse checkouts omitted required validation inputs and PR #24 had left the README digest assertion stale; reference/data/release/dependency review and both CodeQL checks passed. The Gate failed as designed, and the Head is being superseded by the scoped correction.
- Full Viewer E2E and production release validation are intentionally out of scope.

## Plan Deviations and Approval

None.

## Remaining Work and Continuation Conditions

Commit and push the scoped CI-input correction, obtain the new exact Head's applicable GitHub Gates and Deploy Preview, smoke `/`, `/editor/`, `/editor/version.json`, and an Editor static asset, then finalize Completion and mark the PR Ready without merge. If hosted validation exposes Issue #15 scope, stop `partial`.

## Final Delivery

- Record path: `docs/changes/21-editor-source-ownership/completion/task-records/MF-21-T01.md`
- Dossier path: `docs/changes/21-editor-source-ownership/completion/dossier.md`
- SHA-256: pending final hosted-state finalization
- PR / commit / release / deploy: Issue #21 and Draft PR #25 exist; release/deploy/merge prohibited.
