# MF-30 Viewer v1.29.1 conflict register

本登记在 PR 候选准备时关闭全部已知重叠。`Keep / Port / Replace` 是已实施结论；没有“实现时再决定”的项目。长期运行证据见 [`evidence.md`](evidence.md)，版本区间和能力归属见 [`research-supplement.md`](research-supplement.md)，原始浏览器记录仅保存在 `.codex-work/tmp/viewer-v1.29.1/browser/`。

| ID | Area | Metaflow 5.18.1 behavior | Upstream v1.29.1 behavior | Adopted resolution | Final evidence | Residual risk |
|---|---|---|---|---|---|---|
| C-01 | Active source ownership | `metaflow-viewer/` owns route, loading, collision, Analytics, UI, and data compatibility | immutable upstream tree has none of the Metaflow integration | **Port** into active source; never overwrite from snapshot | Six implementation checkpoints plus the final evidence checkpoint modify active source while `references/supersplat-viewer-v1.29.1/` retains its registered digest | Future upstream ports still require behavior-level review |
| C-02 | SH update cadence | released lineage used `colorUpdateAngle = performanceMode ? 4 : 2`; the first local candidate conservatively retained it | `performanceMode ? 1 : 0.2`; v1.29.1 replaces v1.29.0's `2/0` after issue #286 identified zero-threshold every-frame reevaluation | **Replace** with upstream `1/0.2` by explicit 2026-08-13 product decision; retain the earlier `4/2` comparison as evidence, not policy | `cb4a3f16`; source/unit lock; WebGPU/WebGL runtime confirms quality `0.2` and performance `1`. Earlier fixed-camera A/B used 31 frame commits per value; the `2`/`0.2` quality pair measured SSIM `0.999902`, PSNR `65.50 dB` | Smaller thresholds can submit more SH work during small camera motion. Earlier timing was warm-cache/order-sensitive and remains exploratory, not a benchmark |
| C-03 | Render scheduling | first-frame suppression plus a generic four-second continuous-render fallback | load with auto-render, then on-demand via `frame:request` and `renderNextFrame` | **Replace + Port**: adopt upstream phase model and explicit requests; remove four-second fallback | `0d67ac5e`; core tests; all WebGL/WebGPU routes remained live after `autoRender=false` | A newly added dynamic surface could omit an explicit request |
| C-04 | Loading visibility | poster/canvas readiness prevents partial splats | auto-render during load can draw intermediate frames | **Keep** Metaflow visibility contract while loading internally with auto-render | Canvas stayed hidden until first frame on legacy/streaming routes; delayed model/environment run completed without partial exposure | Extreme network or GPU stalls still need production observation |
| C-05 | Streaming parameter timing | local performance state controls budget and reveal/LOD phases | LOD range belongs on `GSplatComponent`; work-buffer parameters, including SH `1/0.2`, precede creation | **Replace + Port**: adopt component/API timing and upstream SH while keeping Metaflow budget/reveal state | `0d67ac5e`, `cb4a3f16`; unit ordering checks; Xunyangpai/Dayun/Bijiashan reached LOD range `0…1000` on WebGPU and Xunyangpai on WebGL | Future engine work-buffer internals may move again |
| C-06 | Partial settings v2 | incomplete post-effect objects such as `{enabled:false}` are valid data | direct upstream access fails without nested defaults | **Keep + Port** normalization before Viewer/post-effect access; no data rewrite | Cyrene legacy SOG loaded with current settings-v2 on WebGPU, WebGL, and mobile emulation | A future upstream effect field needs a matching default |
| C-07 | Annotation UI | branded annotations lacked upstream sticky visibility state | `state.showAnnotations` plus `showAnnotations` localStorage | **Replace + Port** into branded row and nine locales | `773468ac`; five-annotation Xunyangpai toggle hid markers, closed UI, persisted through reload, and was restored | Real assistive-technology traversal not run |
| C-08 | Heatmap URL | one existing `?heatmap` route meaning | same parameter with backend-aware behavior | **Replace** semantics without adding another parameter | Cyrene WebGL emitted one controlled WebGPU-required warning, loaded normally, and omitted voxel overlay; WebGPU overlay regressions passed | No dedicated quantitative heatmap-color fixture |
| C-09 | XR | teleport/smooth/snap navigation, branded dialogs, WebGL reload | backend-aware WebGPU/WebXR detection | **Replace** detection; **Keep** navigation/UI | API/detection and non-XR regression passed on WebGPU and WebGL; unavailable immersive modes kept controls hidden | Immersive session, controller, hand input, and headset rendering remain unverified |
| C-10 | Capture and camera ownership | local camera, animation, post effects, reveal, and on-demand state are mutable | upstream capture swaps target/camera state for supersampled output | **Replace + Port** with serialization and unconditional restoration | WebGPU concurrent default/invalid capture, WebGL post-effect capture, animation scrub, forced texture-read rejection, and subsequent recovery passed; pose and render target restored | Long capture queues on memory-constrained real mobile hardware remain unverified |
| C-11 | Toolchain formatting | Analytics, rrweb, Playwright, and multi-entry output are local | upstream adds Prettier and newer lint/build packages | **Replace + Keep**: align applicable versions and retain local tools; isolate mechanical formatting | `04051b55` dependency/API checkpoint and `1a86d23b` formatting checkpoint; clean install, fmt, lint, typecheck, publint, build pass | Development-only dependency findings require normal maintenance |
| C-12 | Product version | released production remains Viewer `5.18.1` on published baseline `1.26.2` | candidate adds backwards-compatible product/loader/rendering capabilities on upstream `1.29.1` | **Replace release candidate** with Viewer `5.19.0` and upstream `1.29.1`; keep production described as `5.18.1` until separately deployed | package/lock, Version History, public history/index release, Ledger, tests, and current docs are prepared together | Squash merge creates a new implementation SHA; final release-record reconciliation is required before tag/deploy |
| C-13 | Remote governance state | Issue #30 began discovery-only and local implementation was separately authorized | current follow-up authorizes Issue synchronization, push, and detailed PR | **Replace local-only boundary** with `In Review` Issue and draft PR; keep merge/tag/deploy/close outside scope | Remote Issue and PR link the Spec, Plan, research, evidence, commits, risks, validation, and rollback | Remote delivery is reviewable, not released; CI/preview findings may require additional commits |
| C-14 | Transient model failure | one `503` previously left loading at “资源识别” indefinitely | upstream prefetch has no bounded product retry/error UI | **Port local reliability contract**: retry network errors and `408/425/429/5xx` up to three attempts with `500/1000 ms` backoff; do not retry permanent `4xx`; enter explicit refresh error state | `332ad44d`; one `503` recovered on attempt 2, repeated `503` stopped after 3, `404` stopped after 1; terminal state disables `autoRender` and keeps actionable overlay | Retry applies to initial model/environment prefetch, not every streaming/tile child request |

## Rollback points

- Entire implementation rollback target: `dbbd0015a8d13d4380d100fad4e5121dc2b29746` (`origin/main`, Viewer `5.18.1`).
- Last checkpoint before rendering behavior changed: `1a86d23b`.
- Last checkpoint before new public capabilities: `0d67ac5e`.
- Last checkpoint before transient-load recovery: `773468ac`.

Rolling back product code must not delete the immutable reference, review history, or this evidence. Merge/tag/deploy must remain separate: after a squash merge, the release record must be reconciled to the final squash SHA before a `viewer-v5.19.0` tag or production deployment can be authorized.
