# MF-30 Viewer v1.29.1 conflict register

本登记关闭 MF-30 已知的上游、本地与后续决策重叠。`Keep / Port / Replace` 是最终处置；`Original decision source` 如实记录历史来源，不能把实施阶段的 Agent 选择改写成用户当时已经确认。`Final authority` 中没有未经确认的 `agent-implementation`。长期运行结果见 [`evidence.md`](evidence.md)，版本区间和能力归属见 [`research-supplement.md`](research-supplement.md)。完整临时浏览器与测试输出只保存在 `.codex-work/`。

决策来源只使用：`explicit-user`、`approved-plan`、`upstream-default`、`agent-implementation`、`scope-boundary`。

| ID | Area | Metaflow 5.18.1 / early-candidate behavior | Upstream v1.29.1 behavior | Original decision source | Adopted resolution | Final authority | Final evidence | Residual risk |
|---|---|---|---|---|---|---|---|---|
| C-01 | Active source ownership | `metaflow-viewer/` owns route, loading, collision, Analytics, UI, and data compatibility | immutable upstream tree has none of the Metaflow integration | `approved-plan` | **Port** into active source; never overwrite from a snapshot | `approved-plan` | all behavior commits modify active source while `references/supersplat-viewer-v1.29.1/` retains its registered digest | Every later upstream sync still needs behavior-level review |
| C-02 | SH update cadence | released lineage and the first candidate used `performanceMode ? 4 : 2` | `performanceMode ? 1 : 0.2`; this replaces v1.29.0's `2/0` zero-threshold behavior | `approved-plan` | **Replace** with upstream `1/0.2`; retain `4/2` only as historical candidate/A-B evidence | `explicit-user` | `cb4a3f16`; source/unit lock; real WebGPU and WebGL read quality `0.2` and performance `1` | Smaller thresholds may schedule more SH work during small camera motion; exploratory timings are not a benchmark |
| C-03 | Render scheduling | first-frame suppression plus a generic four-second continuous-render fallback | load with auto-render, then use `frame:request` / `renderNextFrame` on demand | `approved-plan` | **Replace + Port** the phase model and explicit dynamic requests; remove the generic fallback | `approved-plan` | `0d67ac5e`; unit ordering; every current real route remained live after `autoRender=false` | A future dynamic surface can regress if it omits an explicit request |
| C-04 | Loading visibility | poster/canvas readiness hides partial splats | loading auto-render can draw internal intermediate frames | `approved-plan` | **Keep** Metaflow visibility while permitting internal load frames | `approved-plan` | legacy and streaming routes kept canvas gated until first frame; delayed subject/environment evidence remains valid | Extreme network/GPU stalls still require production observation |
| C-05 | Streaming parameter timing | local performance state controls budget and reveal/LOD phases | LOD range belongs on `GSplatComponent`; persistent work-buffer parameters precede creation | `approved-plan` | **Replace + Port** component/API timing and final SH `1/0.2`, while keeping Metaflow budget/reveal sequencing | `explicit-user` | `0d67ac5e`, `cb4a3f16`; Xunyangpai, Dayun, and Bijiashan reached complete streaming state | Engine work-buffer internals can move again |
| C-06 | Partial settings v2 | incomplete post-effect objects such as `{enabled:false}` are valid existing data | direct upstream nested access can fail without defaults | `approved-plan` | **Keep + Port** normalization before Viewer/post-effect access; no data rewrite | `approved-plan` | Cyrene loaded current settings-v2 on WebGPU, WebGL, and mobile emulation | Future effect fields need matching defaults |
| C-07 | Annotation UI | branded annotations lacked sticky visibility | `state.showAnnotations` plus `showAnnotations` localStorage | `approved-plan` | **Replace + Port** into the branded row and all nine locales | `approved-plan` | `773468ac`; toggle, tooltip closure, persistence, route stability, and runtime capture passed | Real assistive-technology traversal was not run |
| C-08 | Heatmap URL/backend | one existing `?heatmap` meaning | same parameter with backend-aware behavior | `approved-plan` | **Replace** backend behavior without a second public parameter | `approved-plan` | WebGL emitted exactly one controlled warning and no overlay; actual WebGPU reported `overlayMode=heatmap`, collision ready, and no console warning/error | No quantitative heatmap color fixture |
| C-09 | XR | teleport/smooth/snap navigation, branded dialogs, WebGL reload | backend-aware WebGPU/WebXR detection | `approved-plan` | **Replace** detection and **Keep** navigation/UI | `approved-plan` | API/detection and non-XR regression passed on WebGPU/WebGL; tested browser reported no immersive mode | Immersive session, controller, hand input, and headset rendering remain unverified |
| C-10 | Capture/camera ownership | camera, animation, post effects, reveal, and on-demand state are locally mutable | upstream capture swaps target and camera state for supersampled output | `approved-plan` | **Replace + Port** with a serialized queue and unconditional restoration | `approved-plan` | WebGPU concurrent/default/invalid capture, WebGL post-effect capture, animation scrub, injected failure, and follow-up recovery passed | Long queues on memory-constrained physical mobile devices remain unverified |
| C-11 | Toolchain and formatting | Analytics, rrweb, Playwright, and multi-entry output are local | newer lint/build tools plus Prettier | `approved-plan` | **Replace + Keep** applicable versions and isolate mechanical formatting | `approved-plan` | `04051b55`, `1a86d23b`; clean install, format, lint, typecheck, publint, and builds pass | Development-only audit findings remain normal maintenance work |
| C-12 | Candidate version | production is `5.18.1` on upstream `1.26.2` | the candidate adds compatible APIs, loader, rendering, and package behavior | `approved-plan` | **Replace candidate** with `5.19.0 / upstream 1.29.1`; production stays `5.18.1` | `explicit-user` | product/package checkpoint is `72bd2011`; package, Version History, public release header, Ledger, and tests are reconciled in the following release-support checkpoint | Squash merge creates a new SHA that must be reconciled before tag/deploy |
| C-13 | Remote governance | the original implementation stopped at local checkpoints | the follow-up authorizes same-branch push and detailed Issue/PR updates | `scope-boundary` | **Replace** local-only delivery with Open/In Review Issue and Open Draft PR; merge/tag/deploy remain excluded | `explicit-user` | remote write/readback is part of the final delivery step | Remote review is not a release claim |
| C-14 | Initial subject/environment retry | the first implementation selected three total attempts and `500/1000 ms` without an earlier product-level parameter decision | upstream prefetch has no Metaflow product retry/error-UI policy | `agent-implementation` | **Replace** with four total attempts and `500/1000/2000 ms`; retry only network, `408/425/429/5xx`; permanent `4xx` stops once | `explicit-user` | `0c1a2f6a`; executable tests cover success on attempt 4, exhaustion after 4, body release, permanent 404, terminal UI, and environment isolation | No per-request timeout or `Retry-After`; child streaming/tile/settings/Analytics requests retain their own lifecycle |
| C-15 | Preference lifecycle | startup invoked update paths that wrote inferred performance/Gaming Controls; legacy `retinaDisplay` could influence later behavior | startup reads preferences and user changes write them | `agent-implementation` | **Replace** with one-time `5.19.0` cleanup, read-only initialization, device-derived session defaults, and later state-change persistence; leave Annotation preference intact | `explicit-user` | `4f20ac2c`; desktop/mobile defaults, idempotence, no startup writes, later persistence, and real mobile `storedPerformance=null` passed | The intentional one-time reset must be called out in release notes |
| C-16 | Language configuration | localization read `location.search` internally, so `?lang=` worked but programmatic configuration did not | `Config.lang` feeds localization | `agent-implementation` | **Replace + Port** a single `Config.lang` chain while keeping `?lang=` as the bundled HTML source and navigator/English fallback | `explicit-user` | `f5075412`; configured, URL, case/base-language, navigator, English fallback, and nine-locale parity tests pass | Embedders must set config before Viewer initialization |
| C-17 | Debug Engine | ordinary production resolver only; the earlier review said Port but implementation omitted it | `ENGINE=debug` selects PlayCanvas's development export condition | `agent-implementation` | **Port** the opt-in resolver; production remains the default | `upstream-default` | `8d115d51`; both default and Debug Engine builds pass and produce distinguishable bundle sizes | Debug builds are larger and must never become the deployment default |
| C-18 | Parser/source identity | historical policy let inspected JSON structure override filename and used filename only as fallback | PlayCanvas parser selection is determined by entry identity | `agent-implementation` | **Replace** with `lod-meta.json → streaming-lod`, `.sog → sog-bundle`, other `.json` including `meta.json → sog-meta`, `.ply → ply`; structure validates but never changes parser | `explicit-user` | `d41a28ce`; strict manifest tests, all 9 real manifests, generator check, and exact 87-route/model digest equality pass | Malformed legacy loose SOG JSON still fails in its own parser; it is not reinterpreted as streaming |
| C-19 | Production CSS map | the local Rollup path disabled CSS source maps without a recorded product/build decision | upstream emits CSS maps | `agent-implementation` | **Replace** with composed Sass/PostCSS map, one relative reference, and no local absolute paths | `explicit-user` | `cc0e106c`; source-map-v3 integrity, pack contents, relative source, and path-leak checks pass | Map size is a debug artifact and is excluded from runtime gzip comparison |
| C-20 | DOMPurify advisory | the first acceptance boundary allowed one production moderate finding because only high/critical stopped implementation | the installed PostHog range permits the patched transitive release | `approved-plan` | **Replace risk acceptance** by resolving DOMPurify exactly to `3.4.13` in the lockfile only | `explicit-user` | `bc1f4a9f`; PostHog remains `1.386.8`, `npm ls dompurify` is exact, and production audit is zero | Development dependency audit remains separate and currently reports four high findings |
| C-21 | Package side effects | no declaration; publint suggested one, but the first candidate treated it as out of scope | root/settings exports are pure package values and static public assets are delivery files | `agent-implementation` | **Replace** omission with `sideEffects:false`, only after Node import purity and Rollup/Webpack consumer evidence | `explicit-user` | `72bd2011`; Rollup keeps named/settings exports and drops bare import; packed-tarball Webpack `5.109.2` / CLI `7.2.2` agrees | Any future import-time initialization must update the declaration and fixtures |
| C-22 | SPZ/KHR public support | no route, upload, error, or lifecycle product contract | Engine/parser code may recognize additional formats | `scope-boundary` | **Keep** the public-format boundary; do not advertise or route these formats in MF-30 | `scope-boundary` | no route/index/upload schema or public support statement was added | Reconsider only through a separate product contract and end-to-end lifecycle test |
| C-23 | Current source default and future dual-source labels | `files.model` is the sole authority; six Firefly resources have an unselected streaming candidate but intentionally use SOG | upstream does not provide Metaflow's future data-label/product-choice layer | `scope-boundary` | **Keep** all 87 current defaults. Record a future label system where verified dual-source assets default to `streaming` and expose `highest-quality` SOG as a user choice; implement none of it here | `explicit-user` | generator rerun produced zero route/model drift; counts remain 9 streaming, 78 SOG, with six candidate-bearing Firefly routes still on SOG | Future switching must define release, camera/animation continuity, environment/settings/annotation/collision continuity, first-frame fallback, memory, and persistence |

## Rollback points

- Follow-up decision-completion start: `af63b512030704820ee163632c484c3b754f50a0`.
- Final follow-up product/package checkpoint: `72bd20118708daf72b9c0e0130cd1267b4a0d62c`.
- Last checkpoint before final SH adoption: `874149f4`.
- Last checkpoint before rendering behavior changed: `1a86d23b`.
- Last checkpoint before new public capabilities: `0d67ac5e`.
- Entire implementation rollback target: `dbbd0015a8d13d4380d100fad4e5121dc2b29746` (`origin/main`, Viewer `5.18.1`).

Rolling back product code must not delete immutable reference or historical review evidence. Merge, tag, deployment, and production observation remain separate. A squash merge requires a later release-record reconciliation to its actual final SHA before any stable-release claim.
