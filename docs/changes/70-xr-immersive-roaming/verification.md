# MF-70 验证记录

2026-09-16。当前结论：实现可审查，**完整体验验收未通过，禁止据此宣称可发布**。

## 基线与环境

- 实现从 `origin/main a871786dffdb195399f3e7427987d8db70296491` 隔离；Viewer 5.19.1、SuperSplat Viewer 1.29.1、PlayCanvas 2.21.3。用户主工作区中的其他实验未合并。
- PICO 4 Ultra：OS 5.15.4、PICO Browser 4.0.38、Chromium 125.0.6422.53；USB 调试连接；本地 Viewer 通过端口转发加载公开生产资产，不部署生产。
- 本地入口：`http://127.0.0.1:4180/acg/fireflyfes38/azur-lane?webgl&noanalytics`。现有生产页面作诊断基线。
- 浏览器自动化：Browser plugin not available；使用项目锁定的 Playwright 1.62.1 / Chromium 151.0.7922.34。桌面 1440×900、移动 390×844；额外触控正向对照 1024×768。
- Node 20.19.0 按命令指定，不变更全局 Node/Xcode。依赖锁文件和引擎版本未改。

## 已确认的真机问题

| 项目 | 证据和结果 | 状态 |
|---|---|---|
| 模糊菜单 | 窗口 DPR 1.25，进入 XR 后 DPR 4；引擎将 XR scale 再乘旧 maxPixelRatio / 新 DPR。旧候选每眼只有 510×510。重建原生 scale 1 的呈现层后 framebuffer 为 3840×1920，即每眼 1920×1920；用户反馈文字清楚了 | 根因修复已进入源码；清晰度为真机用户确认，自动重入仍需复核 |
| 场景随头部平移 | 浏览器 `XRViewerPose.emulatedPosition === true`，系统跟踪日志显示 3DoF；旋转存在，真实平移未提供。代码新增状态提示 | 尚未恢复 6DoF，不能算场景稳定性验收通过 |
| WebGPU XR | 真机有 `navigator.gpu`，没有 `XRGPUBinding`；因此当前浏览器可用普通 WebGPU，不具备本轮引擎需要的 WebGPU XR 绑定 | WebGL 降级可进入；保留其他设备的 WebGPU XR 路径 |
| 手部输入 | `XRHand` / `XRJointSpace` 存在，但此前会话未收到实际输入源 | 构造器存在不是手势通过证据 |
| 窗口双手手势 | 真机声明 maxTouchPoints=5。普通页事件探针已安装；尚未收到用户实际双手操作的事件 | 待真机输入，不能判为浏览器不支持，也不能判为 MetaFlow 已适配 |

定位设置名称通过设备已安装设置应用的文字资源核实为“定位追踪”；“躺下使用／斜躺使用”与关闭定位追踪关联。未修改底层系统跟踪属性或替用户设定安全边界。

## 性能：仍未达标

采集的是页面 `frameend` 间隔，仅会话 active 且 visibility=visible 时记样；不是 GPU 时间，不等同于真实移动路线验收。会话报告 72 Hz，对应 13.89 ms 周期。

| 条件 | 样本数 | p50 | p95 | p99 |
|---|---:|---:|---:|---:|
| 生产基线，旧窗口相关分辨率／4M 默认预算 | 7,427 | 97.3 ms | 181.1 ms | 195.2 ms |
| 本地候选，原生 1920×1920/眼、1M 预算 | 261 | 71.8 ms | 84.1 ms | 88.9 ms |
| 同会话诊断，原生分辨率、临时 250K 预算 | 926 | 41.7 ms | 43.7 ms | 52.0 ms |

三行并非同路线、同视角受控 A/B，不计算“性能提升百分比”。250K 只用于诊断，没有作为产品默认值提交。旧候选在 510×510/眼时的高帧率不能用作清晰度修复后的验收。原生分辨率的碧蓝航线场景明显超过刷新预算，仍需定位排序、重叠像素与渲染成本。十分钟采样/30 秒静态录屏不等于十分钟人为连续探索。

## 自动化与浏览器检查

- Node 20 `type:check`、`lint`、`build` 通过。构建仅有既有 Sass legacy API / empty style chunk 提示。
- 新增 XR 行为测试 **15/15**：模拟力度和死区、抬头速度、绕头转向、任意 rig 朝向瞬移、无效地面／墙体／未知分块、相机与运行时恢复、取消与输入移除、菜单隔离、中位重置、分段转向、迟到碰撞显式校准、坐站姿、菜单命中、XR DPR 与单右手柄。测试执行真实 shipping 模块，输入和碰撞使用测试桩。
- Viewer 全量测试 **99/100**。唯一失败是 `upstream-compatibility.test.mjs` 的既有 README 发布状态断言；原 `HEAD` 测试单独执行也复现该失败。未为本轮修改历史发布文字。
- `MCL_SMALL_FIXTURES=1 npm run e2e:build` **8/8**，包含桌面／移动 UI、已有视觉快照、XR 重复请求／授权取消／重试及 AR 失败恢复。XR 浏览器接口为测试桩，不代表头显测试。
- 额外 Chromium 正向对照：两个真实浏览器注入触点拉开使 orbit distance 从 3 变为 1.505，合拢恢复到 2.869；合成 Ctrl+wheel 缩放至 2.181。页面身份正确、画布与控件非空、无页面异常；桌面／移动设置截图已检查。**这证明 MetaFlow 现有两种输入路径有效，不证明 PICO 会发送这两种输入。**
- `git diff --check` 通过。自查与行为 Spec 对照由实现作者执行，不标记独立审查。

## 上游核对

2026-09-16 核对 [SuperSplat Viewer 1.31.2](https://github.com/playcanvas/supersplat-viewer/releases/tag/v1.31.2)：使用引擎 2.22.1。1.29.1→1.31.2 的主要变化是 LOD 首帧设置、动画／嵌入、画布尺寸与加载；没有更新其 XR 导航文件。引擎 [2.22.0](https://github.com/playcanvas/engine/releases/tag/v2.22.0) / [2.22.2](https://github.com/playcanvas/engine/releases/tag/v2.22.2) 含 LOD／排序和 XR 菜单退出隐藏修复；同时存在 API 变更，不能视作无风险依赖替换。本轮复用当前后端和模型支持，修复自己的菜单生命周期，不整体升级上游。

[PICO 4 Ultra 官方开发说明](https://developer.picoxr.com/zh/pico4-ultra/)明确列出 WebXR Hand Input。它未证明普通浏览器窗口会把双手暴露为两个 DOM 触点。PICO OS 6 Spatial SDK 的原生双手缩放文档也不能作为 OS 5.15.4 浏览器能力证明。

## 模拟器与未运行项

Vision Pro 模拟器启动为 visionOS 26.2。已打开本地 URL，但 Safari 被现有原生应用窗口遮挡；自动化可以读取窗口／截图，交互返回 `noWindowsAvailable`，未完成网页选择和 XR 会话测试。未关闭或改动用户现有原生应用。模拟器验证不记通过，Vision Pro 真机舒适度／追踪／性能未验证。

待完成：

1. PICO 恢复真实 6DoF，确认模型不随真实平移移动。
2. 真实单右手柄及手部菜单、移动、舒适模式、追踪丢失／恢复、站坐与重入；验证源码中的原生分辨率修复自动生效。
3. 普通浏览器窗口的单手拖动／双手拉伸事件分类，再决定窗口手势和虚拟摇杆适配。
4. 有碰撞／无碰撞／大型流式场景同路线十分钟 A/B、录屏、实际帧时间；原生分辨率性能先达标。
5. Vision Pro 模拟器输入、菜单、相机与生命周期。

原始截图、静态视频、性能 JSON 与窗口手势对照保存在主仓库未跟踪目录 `.codex-work/artifacts/mf-70/`；包含 `pico-native-resolution.png`、`pico-native-sample.json`、`pico-native-250k-sample.json`、`browser-input-positive-control.json`、`desktop-settings.png`、`mobile-settings.png`。未提交设备序列号、系统日志、安装包、生成构建或缓存。回退为撤回本分支提交；生产从未更改。

## 追加真机反馈：窗口单触点与旧页面重入

同日用户报告已开启“定位追踪”，并试用了普通窗口手势。此前安装的只读探针收集到 980 条画布／摇杆事件：6 次可信 touch pointerdown、2 次 pointercancel、4 次 pointerup，最大同时活动指针数为 **1**，touchstart 的 touches 均为 1，没有 wheel 事件。其中两次出现旧指针 pointercancel 后开始新 pointerId 的情况。结合用户双手操作反馈，这支持**当前配置普通窗口将输入串行化**的判断，但不是所有 PICO OS／浏览器版本均不支持双手的证明。原始记录：`pico-window-hand-events.json`。现有网页双触点缩放正向对照仍通过；沉浸 WebXR 的手部数据是独立通道。

随后用户报告 VR/AR 又模糊。读取当时会话：immersive-ar、XR scale **0.85**、DPR 4、graphics maxPixelRatio 1.25、framebuffer **1020×510**。说明页面仍运行此前尚未刷新的旧代码，临时原生分辨率绑定未跨会话保留；不是已提交 scale 1 代码的重入结果。该会话已出现左手 XR 输入源（hand=true、axes=[]），说明至少该手部输入通道实际可用。

已保存记录、退出该会话并刷新本地页面，新的 manager scale 为 **1**。页面显示 visible / focused，但 requestAnimationFrame 超时、应用 frame=0，初始流式加载等待画面刷新；已请用户戴上头显回到窗口。最终 framebuffer 和新 6DoF 位姿必须等实际 XR 帧后复核，尚不记通过。系统跟踪属性与之前不同，不能仅凭属性值代替 `emulatedPosition` 实测。

## 用户确认：清晰度与定位恢复

用户随后明确反馈：“清晰度和定位没有问题了”。据此将本轮 PICO 清晰度与真实定位的**用户体验复核**记为通过。再次读取时已退出 XR，页面 loaded=true、frame=2164、manager scale=1，普通浏览恢复；由于采样时没有活动会话，未取得本次新的 framebuffer 数字或 `emulatedPosition`，不把用户反馈改写为机器测量结果。

此前 3DoF 和旧页面 510×510 是已定位的历史问题。单右手柄、侧键横移、菜单暂停移动、退出重入的完整操作路线，以及原生分辨率下连续探索性能和窗口单手替代操作，仍需分别验证。已向用户请求下一段右手柄路线反馈。

用户随后再次进入 VR，已取得实际运行时复核：`immersive-vr` / visible，manager scale=1，framebuffer **3840×1920**（每眼 **1920×1920**），`emulatedPosition=false`；识别到右手柄、四轴中位、非 hand 源。因此自动原生分辨率和真实位置追踪均有本次会话的机器证据。此次会话 `frameRate=90`，其性能预算应为 **11.11 ms**，此前 72 Hz 数据只代表此前会话。下一段路线采样已挂接，尚未据此宣称帧时间达标或路线操作通过。

## 单右手柄验收与局部体素叠层

用户明确反馈本轮右手柄路线没有问题：摇杆前后移动、左右转向、侧键加横向横移、菜单打开暂停行走、退出再进入。将这组操作记为用户真机通过；不扩大为站坐、舒适模式、断连或十分钟性能通过。

同次实际后端仍是 WebGL2：navigator.gpu 可用，但 XRGPUBinding 未提供。普通 WebGPU 能力不等于 WebGPU XR。保持原生每眼 1920×1920、真实位置追踪及 90 Hz 会话。两个可见时段帧间隔采样分别为 345 帧 p50 56.2 / p95 69.5 ms，1168 帧 p50 63.6 / p95 97 ms；这不是 GPU 专项计时，也不是受控同路线 A/B，均未达 11.11 ms 预算。原始采样为 pico-right-controller-route.json。

新增 WebGL / XR 局部体素线框：页面碰撞开关与空间菜单共享状态；仅真实已加载网格表面单元，原始尺寸、翻转坐标一致，扫描范围半径各轴 2 m，最多绘制 2000 个单元，单帧扫描最多 2048 个网格且受 2 ms 时间预算约束，关闭后停止扫描。普通 WebGPU 保留已有 compute 叠层。

实际 Chromium 加载“碧蓝航线”WebGL 碰撞数据，通过页面按钮启用／关闭；为查看脚下网格，将诊断相机固定为俯视 55°。截图 voxel-webgl-overlay.png 确认可见三维方块线框，记录 972 个表面单元、23328 个顶点，无页面异常；voxel-webgl-check.json 保存状态。此为桌面真实渲染，不代表 PICO 双眼视觉验收。PICO 本地页已刷新，读取 loaded=true、overlayAvailable=true、scale=1，叠层默认关闭。

本轮类型检查、lint、构建通过；XR 行为测试 **17/17**，增加真实单元边界、翻转与空网格扫描覆盖；浏览器回归 **8/8**。全量 **100/102**：既有 README 发布文字断言仍失败；另有提交历史治理测试在提交 55ced0a 后要求产品提交必须有发布历史 entry，这项是本分支提交后触发，不能归为原基线失败。当前 AGENTS.md 明确 staging 不提升版本，Ledger / Version History 用于发布；本轮未改写测试或伪造发布记录以变绿，治理检查冲突留在 Draft 待处理。

跳跃已建立独立待办 [#72](https://github.com/Shuang-su/Metaflow/issues/72)，包含起跳、重力、安全落地、头顶碰撞、分块缺失与单右手柄按键规则；当前未实现、未承诺日期。继续优先处理原生清晰度下的性能、剩余设备与多场景验收。

刷新后 PICO 已重新进入真实 VR：读取叠层启用、2000 个单元上限、48000 个线段顶点、扫描结束，framebuffer 仍为 3840×1920。pico-voxel-overlay.png 确认新菜单含碰撞叠层开关；当前向上视角仅在边缘看到部分线框，不能据此确认完整双眼位置一致性，已请用户查看脚下与近处实测。为本次复核临时开启叠层，用户可在菜单关闭。

## 交互优先：模式入口、反馈与恢复

用户反馈“看不到舒适模式的切换开关”，随后明确流畅度暂时没有明显问题，先完善交互与沉浸体验。本轮停止继续专项性能调优；先前指标保留为工程记录，当前用户优先级以交互为准。

修正菜单可发现性：原“移动方式：连续移动”状态式标签，改为“切换到舒适模式（瞬移）”；切换后显示“切换到连续移动”。站坐按钮也明确下一步动作；标题区持续显示当前模式、姿态与暂停行走状态。底部按单右手柄、双手柄、手势以及是否存在地面选择提示。按住按钮提供颜色反馈；成功菜单选择／有效瞬移尝试 0.2 强度、35 ms 震动，硬件缺失、拒绝或断连均不影响操作。实际手柄震动与手势输入未据此记为通过。

修复位姿丢失恢复：PlayCanvas 在没有头部位姿时跳过 XR update，因此仅在普通 update 内检查时间不足以发现恢复前的中断。现在在新有效帧覆盖时间戳前检查超过 250 ms 的间隔，清除旧手势、等待摇杆回中；选择事件要求最近有效动画帧与目标射线位姿；下面补充记录事件帧检查的真机回归及修复。行为测试覆盖空头部位姿、恢复时仍推住摇杆、回中重新启用，以及被引擎跳过的帧间隔。

修复近裁切回退：真机 entry 跟踪记录显示，引擎初始 attachPresentation 传入 depthNear=0.03；重建原生分辨率时，相机 getter 却返回第一帧缓存的 0.1，再次 attachPresentation 将浏览器裁切改回 0.1。明确使用 XR 的 0.03 / 1000，并同步引擎 XR 裁切元数据，保留浏览器 FOV 与真实头部位姿。旧页面直接 updateRenderState 的诊断已证实左右眼投影可以变为约 0.0300000015；新构建自动生效仍等待新真机帧，不能把临时诊断写成新版本验收。

检查：19/19 行为测试、10/10 浏览器回归、类型、lint、构建通过。新增浏览器测试通过真实 Viewer 空间菜单的射线选择路径执行连续→舒适→坐姿→连续→继续，检查按钮文本、偏好与面板开闭。此为普通 Chromium 内的空间菜单测试适配，不模拟头显光学、原生追踪或硬件。Browser plugin not available，使用项目 Playwright；中文菜单实际绘制截图为 xr-menu-continuous.png、xr-menu-comfort.png，页面身份正确、非空、无框架错误或 pageerror。全量测试仍保留两项文档／发布历史失败，未改写治理测试或发布版本。

设备连接曾丢失 ADB 端口映射，同时本地预览服务停止，PICO 标签显示离线页；已恢复映射和服务并重新打开本地 URL。当前页面尚未收到第一帧（frame=0），已请用户戴上头显返回标签页。头显新菜单、3 厘米自动裁切、舒适模式、手势与追踪恢复的本轮真机验收仍待完成。

### 暂缓前的负载对照

本地原生分辨率、关闭调试叠层、同一站立区域的三个约 9 秒样本：1M 预算 120 帧，实际约 979953 splats，帧间隔 p50 77.6 / p95 78.6 ms；250K 预算 198 帧，实际约 282217 splats，p50 44.5 / p95 47 ms；100K 预算 202 帧，实际 splats 仍为 282217，p50 44.5 / p95 46.1 ms。降低预算不保证现有资产实际数量继续下降。包裹 app.update / app.render 的主线程墙钟耗时中位数约 0.1 / 0.5 ms，PICO 未提供 GPU timer 扩展；由此只推测绘制侧压力较大，不声称测到了 GPU 耗时。不是同路线受控 A/B。原始数据 pico-native-load-comparison.json。临时预算已恢复 1M；叠层关闭用于采样，用户随后可自行开关。

全量命令补充区分：直接 `npm test` 为 101/104，另有 Dayun 完整数据检查因隔离工作树只有 LFS 指针而失败；使用此前同样的小夹具模式 `MCL_SMALL_FIXTURES=1 npm test` 为 102/104，保留上述两项文档／历史失败。小夹具通过不代表大型真实 Dayun 数据完整性检查通过，未绕过断言或改动数据。

## 舒适模式输入回归修复与双眼线框修复

新菜单上机后，用户报告扳机和摇杆失去响应。定位到本轮新增检查错误地在输入事件帧调用 getViewerPose；[WebXR 规范](https://www.w3.org/TR/webxr/#dom-xrframe-getviewerpose)规定它仅能用于动画帧，输入事件帧抛出 InvalidStateError，被旧检查当作位姿无效而拒绝菜单选择。现在仅用最近有效动画帧的时间戳检查头部追踪，输入事件帧只调用 getPose 查询目标射线。测试替身改为在事件帧 getViewerPose 时抛出同样异常，避免原替身掩盖问题。现场修复后用户明确确认“选择和转向已恢复”。这不代表瞬移落点、站坐、断连和完整路线均已通过。修复已提交为 61c27441。

新构建的近裁切已取得真实双眼投影证据：session depthNear=0.03，相机及左右眼投影近面约 0.0300000015；pico-native-clip-confirmed.json 保存结果。覆盖此前“自动生效待验证”的状态。

原体素线框只在一眼显示，用户闭眼对照确认“只有左眼”。刷新清除临时包装后仍复现。两个眼视图都有绘制调用，视口、矩阵和 GL 错误检查未发现异常；切换图元、材质、图层或重新绑定顶点缓冲不能解决。对照同一批坐标：每次绘制 256 个单元（6144 顶点）时双眼出现，512 个单元及以上时该设备丢失一眼；重复同一可见单元至 2000 个也复现，因此不能归因为场景位置。此为当前 PICO / 浏览器 / 引擎组合的实测边界，未断言所有设备均如此，也未定位到底层驱动的具体错误。

改为每批最多 256 个完整单元，最多八批保留全部 2000 个单元。每批有自己的 Mesh 和 Render Entity，重用缓冲、隐藏缩小扫描后多余批次，关闭父实体停止显示和扫描，销毁父实体释放子实体及网格。无引擎升级、无永久绘制钩子、无减少碰撞数据。现场八批共 48000 顶点的原生截图已出现完整双眼线框；世界位置固定是预期行为。分批完整性与边界自动化不代替真实双眼证据。

本次最终代码检查：XR 行为测试 **20/20**、Playwright 桌面／移动端 **10/10**、类型、lint、构建通过。桌面真实 WebGL 碰撞场景在已知有碰撞坐标检查八批共 2000 个单元；关闭后不显示／不扫描，再开启复用全部网格，销毁应用后批次与子实体全部释放，pageerror 为空，记录 voxel-batches-lifecycle.json / voxel-batches-webgl.png。最初任意相机坐标没有扫描到表面，等待非空叠层超时；改用已知有碰撞坐标后通过，不把空扫描视为有几何的正向对照。

PICO 已再次刷新并重入，确认正式源码为八批（七批各 6144 顶点、末批 4992 顶点）、完整 2000 单元，事件检查为修正后的实现，depthNear=0.03，无临时绘制包装。pico-stereo-shipping-state.json 保存代码／运行状态。当前朝向恰好没有采样线框落入视锥，pico-stereo-shipping-facing-away.png 不作为双眼视觉通过；现场分批方案双眼可见的原生截图单独保存为 pico-voxel-stereo-batch-diagnostic.png。已请用户转身／低头复核正式构建及实际瞬移，未收到反馈前保留此验收项。

## 脚下体素覆盖修复

用户反馈位置不在脚下。实测头部世界位置与碰撞向下射线一致，原线框并非相机空间偏移：原扫描按 z/y/x 顺序从范围边缘取样，在 2000 单元上限前只取到相机约两米外的一侧，遗漏脚下。改为先扫描头部投影所在列，然后按水平同心方环扩展；已加载碰撞分块轮流推进，保留空单元 yield 与原时间／数量预算，不改变单元坐标、碰撞数据或生成地面。

新增回归构造超过 2000 单元的平面，检查首列覆盖脚下、环序扩展、无重复／遗漏，以及两个分块均获扫描机会；行为测试 **21/21**。类型检查、lint、构建通过；实际 Chromium 八批几何、开关、复用及销毁检查再次通过，无 pageerror。此前 10/10 UI 回归属于前一检查点，本次未改菜单／会话路径。

已刷新 PICO 并重入正式构建：2000 单元／八批时 footCovered=true；头部约 (-8.232,0.523,-2.619)，向下射线地面 y=-0.64，显示单元 (-8.24,-0.72,-2.64,size=0.08) 正好覆盖脚下且顶面与查询地面一致。记录 pico-voxel-foot-coverage.json。截图 pico-voxel-foot-first.png 为平视偏上的画面，不作为脚下视觉证明；已请用户低头与移动头部复核贴合，反馈待收。

## 对齐 Viewer 步行间隙与接触处理

用户明确要求离地间隙按 Viewer，并补充选中地面平均／碰撞推出与滑动。XR 间隙从 2.5 cm 改为 20 cm；进入、校准、回起点、姿态切换、瞬移和步行使用同一常量。步行从“五个点必须全部命中、采用最高点”改为对有效命中的高度与向上法线取平均，容忍单个体素边缘的异常坡度；分块未知、全无支撑、陡坡、超过 25 cm 的高差仍拒绝。胶囊接触尝试有界推出，修正后重新探测地面和碰撞；水平位移不能超过用户请求，向下推出／深穿透／未解决接触仍停止。没有复制 Viewer 的重力、弹簧或惯性；瞬移和进入的落点验证保持严格。

新增回归覆盖与实际 WalkController.hoverHeight 一致、进入／瞬移／重置／坐姿高度与真实追踪分离、小孔地面平均但瞬移拒绝、斜墙滑动、修正后跨入未知分块拒绝、撞顶及未解决碰撞拒绝、小接触推出与下一步高度稳定。行为测试 **27/27**，类型、lint、构建通过；菜单／生命周期浏览器回归 10/10。

在 PICO 当前已加载真实场景上，只读调用前后两个版本的移动函数，从相同头部位置／地面向八个方向各请求 0.4 m，不改变相机或 rig。两方向旧结果约 0.2 / 0.28 m，新结果均 0.4 m；其余方向水平距离相同，地面平均产生约 0–3.2 cm 的高度差。记录 pico-walk-collision-ab.json。这是同碰撞输入的计算对照，不替代真机路线舒适度或性能验收；尚待用户走过原卡点复核。

最终源码构建已在 PICO 重入：continuous / standing，头部 y=0.87781996、floor=-0.48、trackedHeight=1.15782005，差值为 **0.19999991 m**；仍为 3840×1920、depthNear=0.03。记录 pico-walk-shipping.json。刷新时本地连接映射丢失已恢复，当前会话正常。已请用户试走小坑、斜靠围栏并反馈高度跳动或穿透；此项真实路线尚待回复。

## 用户认可步行调整；继续瞬移、姿态与恢复

用户反馈“可以的”，随后明确继续推进。记录为对上一轮步行调整的体验认可，不扩大为全部路线、性能、瞬移或断连验收通过。

舒适模式补充未按扳机时的落点预览，优先右手柄，仅绘制真实校验通过的青色落点环；无效目标显示红色射线，指向菜单唤出控件时不叠加瞬移射线。实际 select 才提交瞬移，预览不移动相机。

修正站坐切换及坐姿重校准先改变高度再检查碰撞的问题。站坐在当前地点验证加载状态和胶囊净空；校准先找目标，成功后一次提交高度偏移与 rig 位置，失败保持之前的位置、偏好及有效校准状态，显示提示。

最后一个手柄断开会清除选择、显示恢复菜单；重连仍需继续并让摇杆回中。重连或追踪恢复时仍按住的扳机／菜单键等待释放，不重放旧选择；瞬态手部源移除不强制弹菜单。回归覆盖阻塞姿态、校准失败原子性、断开期间旧 select、重复绑定、重连长按、回中恢复、无按键预览／菜单互斥和合法／无效落点绘制。

本轮行为测试 **31/31**，类型／lint／构建通过，Playwright 桌面及移动端 **10/10**（包含真实菜单射线选择后姿态受阻时保持偏好并显示状态）。实际中文菜单绘制截图 xr-posture-blocked.png / xr-input-disconnected.png 可读，无 pageerror，记录 xr-recovery-menu.json；普通浏览器适配不代表原生头显验收。

PICO 调试映射恢复后已刷新并进入正式新构建：active=true，预览与按键释放保护已加载，3840×1920、离地约 0.2 m 保留。采样时控制器输入源为零，不能据此声称已看到硬件落点预览或完成断连恢复。记录 pico-recovery-runtime.json；已请用户依次测试未按扳机的舒适落点预览／瞬移，以及空旷处站坐切换。物理手柄断连、手部及 Vision Pro 仍待验证。

## 菜单瞄准落点与入口稳定性

将菜单固定长度射线改为终止于面板实际交点，附加半径 6 mm 的世界空间圆环。留白区显示交点但不作为可选行，背面／面板外不显示圆环。收起后的入口在有效射线瞄准或按住期间固定，脱离后恢复跟随，失效射线不能锁住入口；完整菜单保持唤出位置。没有添加镜头运动或调整步行参数。

新增旋转／非均匀缩放面板交点、留白／背面／落空、圆环尺寸、瞄准／按住冻结及失效输入释放回归。首次新增测试夹具遗漏面板宽高导致 NaN，补齐夹具后 **33/33** 行为测试通过；类型、lint、构建通过。Playwright XR 6/6 与 Viewer 4/4（桌面／移动端共 **10/10**）通过。中文菜单真实绘制 xr-menu-pointer.png 已目视检查，圆环贴合高亮按钮、射线终止于交点，pageerror 为空，记录 xr-menu-pointer.json。未重跑全量套件，原有全量检查限制保留。

刷新时本地预览服务停止导致 PICO 离线页，恢复服务后重新加载并进入真实 VR。pico-pointer-runtime.json 确认新交点实现已加载、active=true、3840×1920、离地约 0.2 m；采样时输入源为零，不作为硬件瞄准通过。已请用户复核圆环及小菜单入口瞄准稳定性；前轮实际瞬移、站坐、断连与手势验证仍待反馈。

## 瞬移首个表面与无效落点反馈

补齐瞄准射线首次命中表面的向上法线检查（仍保留 45° 容差）：墙面、顶面、过陡及非有限法线拒绝；通过后继续原完整足迹／净空／分块校验。共享 inspectTeleportTarget 给预览与实际提交，复制碰撞复用对象中的坐标。无效命中保留障碍位置，红色射线止于命中点并绘制叉号；无命中仍只显示短红线，不伪造落点。按住瞬移期间指向菜单入口隐藏瞬移预览，并取消该次瞬移确认，避免操作菜单时移动。

新增墙边下方可站立但首次表面不可站立、顶面／异常法线、有效首表面、红叉／射线终点／不移动头部，以及瞬移转向菜单后取消且后续正常确认的回归。首次测试夹具误用了无碰撞手势自由移动状态，改为已加载碰撞状态后 **36/36** 行为测试通过。类型、lint、构建通过，Playwright 桌面／移动端 **10/10**。未重跑全量 npm test，既有治理／数据缺口继续保留。

通过项目 Playwright（Browser plugin 不可用）在 1280×900 普通 WebGL 页面绘制真实预览，使用合成墙面查询验证显示：xr-teleport-blocked.png 中红叉与射线终点重合，无 pageerror，记录 xr-teleport-blocked.json。这是渲染检查，不代表真实场景墙体、头显双眼或体感验收。

PICO 已载入并重入正式新构建：active=true，新红叉实现已加载、一个有效输入源、3840×1920、离地约 0.2 m。记录 pico-target-runtime.json。已请用户检查地面青环／墙面红叉，以及按住瞬移后转向菜单入口不发生移动；尚未收到真机体验反馈。

用户针对本轮 PICO 测试明确回复“是的”：确认地面青色落点环、墙面红叉且不能瞬移，以及按住扳机转向菜单入口再松开没有意外移动，以上三项记为真机通过。此反馈不扩大为站坐、断连、完整手势、长路线或性能验收。

## 补齐连续步行小起伏过滤

用户指出仍缺起伏过滤，核对属实：XR 原实现仅五点平均；当前 Viewer 使用弹簧阻尼而非此前口头描述的固定 2.5 cm 过滤。更正该描述。XR 新增无惯性的 2.5 cm 高度容差，平均地面与上次软件支撑高度的差超出范围时只跟随超出部分，连续坡道不会因单帧差小而停在原高度，也不会周期性一次跳完整阈值。名义 20 cm 间隙保留，相对实测平均地面允许 ±2.5 cm 波动。身体净空在实际过滤高度检查，接触推出优先；不滤瞬移／校准或真实头部追踪。

行为测试 **37/37**：新增交替小起伏高度不变、持续上／下坡每步跟随且累计正确、过滤位置撞顶仍拒绝；原接触推出、地面缺失、分块、墙体滑动与进入／瞬移高度测试均通过。更新地面平均测试的预期以包含容差。类型／lint／构建通过；本次未改菜单或会话路径，10/10 浏览器回归属于上一瞬移检查点，未重跑。头显小起伏体感尚待验证。
