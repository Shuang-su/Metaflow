# MF-76 验证与边界

2026-09-17，实验状态，联合 [MF-75 验证](../75-xr-hands/verification.md)。

已实现：仅 collisionStatus=unavailable 进入自由选点；世界射线拾取已驻留高斯，舒适模式重新查询瞬移，连续模式保持靠近/释放停止。头部到目标≤10m、保留0.75m、任意高度、不改变朝向。标记为观察目标，不生成站立地面。手部默认瞬移，目标靠近和朝头部方向走为互斥设置。

查询限10Hz预览，单一GPU请求；提交排队后新查。菜单、追踪/输入/会话变化取消旧代次；正常瞬态捏合结束保留已确认请求。读回晚于松手不能启动连续运动。未知碰撞和加载中不进入自由选点。

本地通过：3D距离与停靠、异步取消、单查询排队、瞬态结束与控制器断连差异；浏览器真实高斯深度命中，头部位置/朝向不变。定向绘制回归额外检查偏离头部的世界射线及临时相机/驻留映射/可见性回调的恢复。

实现限制：PlayCanvas2.21.3统一高斯把绘制对象绑定到Viewer相机。查询相机在同步拾取通道借用现有驻留集合并临时接入world layer及可见性回调，finally恢复，不建立新的LOD/流式加载中心。此内部接入依赖当前引擎；未来升级需重验。透明/稀疏表面和未驻留数据可能没有可靠深度，无命中保持原位；自由选点不保证路径无穿墙。

PICO三类场景各10分钟、双眼位置和实际查询/帧时间未验，Vision Pro模拟器本轮已显示沉浸菜单，但点击工具返回noWindowsAvailable，真实选择/生命周期仍未验，WebGPU XR未验；不可把浏览器fixture通过作为这些项通过。当前保留实验/草稿，不发布。
