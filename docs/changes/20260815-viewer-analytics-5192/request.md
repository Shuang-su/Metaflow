# Request

本次请求按用户原始顺序记录：

1. 查看数据库后台，核对用户关于 iPad Viewer 的反馈。
2. 判断浏览器是否可能限制点击或拖移，造成用户行为误判。
3. 核对最近（今天或昨天）的埋点是否正常，确认是否被最近更新移除。
4. 恢复埋点；如果发布有问题，使用 Viewer `5.19.2`。
5. 继续确认是否还有类似问题，以及是否可以不改版本号直接发布。

反馈截图描述的是 iPad 上查看时容易触屏转着转着偏离中心、随后跑到其他地方，以及模型精度应当足够的体验问题。本 Change 只处理已确认的 production analytics 配置缺失，不把未证实的 iPad 输入误判当作本轮发布内容。
