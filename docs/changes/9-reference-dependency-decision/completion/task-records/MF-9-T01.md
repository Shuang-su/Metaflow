---
task_id: MF-9-T01
change_id: MF-9
tool: codex
source_task_id: 019fe70e-e5e7-7b81-ae68-39eb0a087128
status: complete
started: 2026-08-09T20:21:00Z
completed: 2026-08-09T20:46:23Z
branch: codex/mf-9-reference-dependency-decision
head_before: 91167cc3e2c2f8ceb80cbb07644aeb927b5320c9
head_after: cc224f63ea43c1f57756097d45401a2ce7b7c5aa
plan_revision: 1
plan_sha256: e9d4cc1981d2f2566d77dc2440db9620165876bae3f8327b23227885d33c7db6
archive_status: complete
execution_topology: single-agent
instruction_authority: task-local
authority_source: User-approved Editor migration, upstream restoration, and dependency PR disposition plan
implementer_id: codex/019fe70e-e5e7-7b81-ae68-39eb0a087128
review_relationship: author-self-review
reviewer_id: codex/019fe70e-e5e7-7b81-ae68-39eb0a087128
control_evidence: null
---

# Agent Completion Record

## Authorized Scope

For MF-9 only: update Issue #9 and MF-2, leave tailored comments on PR #4–#8, close them without merge, re-read external state, and create, publish, validate, and merge a governance-only closure archive. Product, snapshot, dependency, Netlify, release, and production changes are excluded.

## Execution Method and Authority

- Execution topology: single primary Codex Agent; no subagent was used.
- Tool-specific instructions and source: GitHub connector for issue/PR writes and local Git for branch, commit, push, and validation, authorized by the current user request.
- Adoption level: task-local MCL pilot.
- Scope of authority: MF-9 decision and closure only.
- Review relationship: the author performs separate Spec-compliance and code-quality self-review passes; no independent non-author review is claimed.

## Complete User Request

``````markdown
---
change_id: MF-9
task_id: MF-9-T01
source: codex-user
source_thread_id: 019fe70e-e5e7-7b81-ae68-39eb0a087128
captured: 2026-08-10
message_count: 1
redactions: 0
---

# Complete User Request Transcript

<!-- user-message:1 -->
PLEASE IMPLEMENT THIS PLAN:
# Editor 源码迁移、上游快照恢复与依赖 PR 处置计划

## 1. 执行结果与固定边界

本轮按三个串行 Change 执行：

1. 关闭 PR #4–#8，并归档 Adopt/Skip 决定；
2. 产品 PR A：将 `metaflow-editor` 改为活动源码目录，恢复完整上游 `supersplat-v2.28.0` 快照；
3. 产品 PR B：只修复活动 Viewer/Editor 的相关依赖漏洞。

两个产品 PR 均以“Ready、Preview 和适用 Gate 通过”为终点：

- 不合并产品 PR；
- 不创建正式版本；
- 不执行生产部署；
- 不把 Viewer 完整 E2E 或产品发布验证设为硬门槛；
- 如果 Preview 因既有 Netlify/LFS 问题失败，只报告 `partial`，不得把无关基础设施改造并入产品 PR。

全部修改使用从 `origin/main` 创建的独立 worktree。禁止更新、rebase、reset 或 merge 当前本地 `main`，并禁止触碰未跟踪的 Swiftgram 和 MCL 前身计划。

## 2. PR #4–#8 与 Issue #9

### 2.1 决策矩阵

| PR | 原目标 | 决定 | 后续 |
| --- | --- | --- | --- |
| #4 | 在 `supersplat-viewer-v1.18.2` 升级 `js-yaml` | Skip 并关闭 | 活动 Viewer/Editor 不包含 `js-yaml`，不移植 |
| #5 | 升级 `immutable` | 原 PR Skip 并关闭 | 在产品 PR B 将活动包升级到 `5.1.9` |
| #6 | 升级 `postcss` | 原 PR Skip 并关闭 | 在产品 PR B 将活动包升级到 `8.5.26` |
| #7 | 升级 `concurrently`/`shell-quote` | 原 PR Skip 并关闭 | 在产品 PR B 升级至 `concurrently 9.2.4`、`shell-quote 1.9.0` |
| #8 | 升级 `fast-uri` | 原 PR Skip 并关闭 | 在产品 PR B 将活动锁文件解析到 `3.1.5` |

所有原 PR 均禁止合并，因为其目标是不可变的版本化参考快照。

### 2.2 执行顺序

- 先创建活动依赖安全 Change/Issue，记录目标包、GitHub Advisory、活动路径和不修改参考快照的边界。
- 更新 Issue #9，写入上述逐项决定、依据和后续 Change 链接。
- 分别在 PR #4–#8 留下针对性说明，然后关闭 PR；重新读取并确认：
  - 状态为 `CLOSED`；
  - 没有 merge commit；
  - 快照分支内容未进入 `main`。
- 在 `docs/changes/9-...` 形成 MF-9 Completion Dossier。该治理归档使用独立 PR，在 Gate 通过后可以合并并关闭 Issue #9；它不得包含产品源码或依赖修改。

## 3. 产品 PR A：Editor 源码所有权迁移

### 3.1 目录与接口

创建 T3、明确指定为 MCL 试点的 Editor Source Ownership Change：

- `metaflow-editor/` 成为活动 Editor 源码包；
- `metaflow-editor/dist/` 为构建产物并保持 Git 忽略；
- `supersplat-v2.28.0/` 恢复为不可修改的官方上游快照；
- `/editor/`、导出格式、用户文件兼容和 Editor 版本 `1.1.0` 均保持不变。

活动 Editor 包设置为：

- package name：`metaflow-editor`；
- version：`1.1.0`；
- `private: true`；
- 保留上游 MIT 许可证、来源和本地定制说明。

组件注册表改为：

- `editor` 拥有 `metaflow-editor/**`；
- `reference` 拥有 `supersplat-v2.28.0/**`；
- 路径所有权仅决定检查路由，不授予 Dependabot 修改参考快照的权限。

### 3.2 无损迁移与上游恢复

- 迁移当前定制源码以及全部行为，包括：
  - Metaflow 品牌与版本信息；
  - Legacy ZIP、settings JSON、HTML/SOG package；
  - Service Worker 和 locale；
  - 100000 帧时间轴限制；
  - 现有 Viewer 依赖及导出/重新打开契约。
- 删除当前受版本控制的编译产物，再从活动源码构建 `metaflow-editor/dist`。
- 使用官方 `playcanvas/supersplat` `v2.28.0` 恢复快照，固定：
  - tag object：`ca76baf0c6b7f12a337c1c71a37554eb991a25f9`；
  - commit：`9f4dfe1ff4e94876fb2054353497c8e2eb93b423`；
  - tree：`0ce0d79143abc945e394d1f13f362533a15bf363`；
  - 文件数：232；
  - 规范化树 SHA-256：`9d37961e3ba6259b26f8564e177d5aa4de7d547caa2fd6a8a7ae748c1a6df4a7`。
- 新增机器可校验的 Reference Snapshot 注册信息，CI 根据路径、mode、blob 和规范化摘要验证快照；任何额外、缺失或修改文件均失败。

### 3.3 元数据、构建与部署

- 当前 Editor 元数据改为：
  - `current.sourcePath = "metaflow-editor"`；
  - `current.upstreamSnapshotPath = "supersplat-v2.28.0"`。
- 历史 `1.1` 条目保持原路径和原 Git ref，不伪造其历史；Ledger 追加迁移说明。
- 不新增 Editor Version History 发布条目，因为运行行为和版本没有变化。
- 版本生成器继续维护数据镜像，但运行时文件改为生成到 `metaflow-editor/dist/version.json`。
- Editor CI、Release 和契约测试从 `metaflow-editor` 安装、测试、lint 和构建。
- Netlify 在构建时生成 Editor `dist`，再发布到 Viewer 的 `/editor/`；不再读取仓库中的预编译 Editor。
- PR A 暂不增加 Editor Dependabot 目标，避免在受控依赖修复前自动生成新 PR。

### 3.4 PR A Gate

必须通过：

- 上游快照 commit/tree/file count/digest 完全匹配；
- Editor `npm ci`、测试、lint 和 build；
- Legacy ZIP、settings JSON、HTML/SOG package、帧数限制和品牌/version 契约；
- 构建前后运行时文件清单及内容对比；允许 source map 和预期的版本元数据路径变化，其他差异必须停止；
- 相关治理、元数据、Markdown、Completion 和 `git diff --check`；
- Netlify Deploy Preview 可访问 `/`、`/editor/`、`/editor/version.json` 及 Editor 静态资源。

不要求完整 Viewer E2E、视觉基线、性能采样或生产发布验证。Preview 和适用检查通过后将 PR A 标记 Ready，但不合并，Change 保持 `ready-for-release`。

## 4. 产品 PR B：活动依赖安全修复

PR B 从 PR A Head 创建，并以 PR A 分支为 base，形成可独立审查的 stacked Ready PR。以后只有在 PR A 获准合并后，才允许将 PR B retarget 到 `main` 并重新运行全部 Gate。

仅修改活动包：

- `metaflow-viewer`；
- `metaflow-editor`。

固定升级结果：

- 两个活动包的直接 `postcss` 为 `8.5.26`；
- 两个活动包的直接 `concurrently` 为 `9.2.4`；
- 锁文件中的 `immutable` 为 `5.1.9`；
- 锁文件中的 `fast-uri` 为 `3.1.5`；
- 锁文件中的 `shell-quote` 为 `1.9.0`；
- 不增加或升级 `js-yaml`；
- 不修改任何 `supersplat-v*` 或 `supersplat-viewer-v*` 快照。

在此 PR 中才将 `/metaflow-editor` 加入 Dependabot 的活动 npm 目录。

PR B 必须验证：

- 锁文件精确解析为上述版本；
- 对应 GitHub Advisory 已不再适用于活动依赖树；
- 未引入新的直接依赖或意外 major 升级；
- Viewer 执行 `npm ci`、test、typecheck、build；
- Editor 执行 `npm ci`、test、lint、build；
- 构建产物如出现非预期运行时代码变化，必须停止并调查；
- Preview 对 `/` 和 `/editor/` 执行基础 smoke；
- 不把无关的既有 audit 警告伪报为本 Change 已解决。

通过适用 Gate 后将 stacked PR 标记 Ready，但不得合并、retarget、发布或部署到生产。

## 5. 完成记录与停止条件

每个独立任务必须保存完整用户问题、获准计划、行动与回复摘要、验证、副作用和真实状态。

最终状态应分别为：

- MF-9：PR #4–#8 已关闭、决策已归档后可以 `closed`；
- Editor 迁移 Change：PR A Ready，但未合并，状态 `ready-for-release`；
- 依赖安全 Change：PR B stacked Ready，等待 PR A，状态 `verifying` 或 `ready-for-release`，不得 `closed`。

发生以下任一情况立即停止并报告 `partial`：

- 上游快照无法精确重建；
- 当前 Editor 定制行为无法无损迁移；
- 运行时产物出现未获准差异；
- 目标依赖仍落入已知受影响范围；
- required check 或 Preview 失败；
- 为通过 Preview 必须扩大到 Issue #15 的 LFS/Netlify 基础设施改造；
- GitHub 状态无法事后重读；
- 当前本地 `main`、未跟踪 Swiftgram 或 MCL 前身计划发生变化。
<!-- /user-message -->
``````

## Complete Effective Task Plan

``````markdown
---
change_id: MF-9
title: Close snapshot dependency PRs and archive the decision
status: implementing
component:
  - reference
risk: T3
type: upstream-sync
owner: Shuang-su
created: 2026-08-10
updated: 2026-08-10
issue: https://github.com/Shuang-su/Metaflow/issues/9
plan_revision: 1
completion_state: pending
supersedes: null
terminal_reason: null
---

# Implementation Plan

## Goal

Close PR #4–#8 without merging or modifying any versioned snapshot, route applicable Active dependency work to MF-2, and merge a governance-only MF-9 closure archive.

## Architecture summary

Issue #9 and the immutable Task Record are the decision sources. GitHub comments and states are external effects. Evidence and Completion are repository records. MF-2 is the only continuation for product dependency changes.

## Preconditions and dependencies

- Work only in `/Volumes/Prism/Metaflow-mf9-decision` from `origin/main`.
- Preserve `/Volumes/Prism/Metaflow` local `main`, its nine local commits, and untracked research.
- Confirm PR #4–#8 are open, unmerged, and target only the versioned snapshot.
- Reuse existing Issue #2 as MF-2 because it already owns residual Active Viewer/Editor npm advisories; update it instead of creating a duplicate.

## Branch and worktree strategy

Use `codex/mf-9-reference-dependency-decision`, commit only `docs/changes/9-reference-dependency-decision/**`, push it, and open a governance-only PR against `main`.

## Tasks

### MF-9-T01 — Decision, external state, archive, and merge

- Authorized scope: update Issue #9 and MF-2; comment on and close PR #4–#8; create, validate, publish, and merge the governance-only MF-9 archive.
- RED boundary: confirm the five PRs are open and MF-9 has no closure archive.
- GREEN implementation: record the decision matrix, update MF-2, close each PR without merge, re-read state, and generate Completion.
- REFACTOR boundary: documentation and deterministic archive cleanup only; no product, dependency, snapshot, Netlify, or release changes.
- Validation: MCL check, platform/link checks, `git diff --check`, PR diff inspection, GitHub check suite, merge-state and main-state re-read.
- Commit and PR boundary: one governance commit and one squash-merged governance PR.
- Serial dependency: complete this Task before beginning Editor migration or MF-2 product changes.
- Rollback and stop: if any PR is merged, target scope differs, a required check fails, or the diff touches a snapshot/product path, stop with `partial` and do not merge the archive PR.

## Final validation

- PR #4–#8 each report `CLOSED`, `merged: false`, and `mergedAt: null`.
- Issue #9 contains the approved matrix and MF-2 link.
- MF-2 contains exact Active package targets and explicitly excludes `js-yaml` and snapshots.
- The archive PR changes only MF-9 documentation and passes required governance checks.
- The local `main` SHA, untracked predecessor plan checksum, and Swiftgram presence remain unchanged.

## Release, observation, and closure

No product release or deployment applies. After the archive PR merges and its main state is re-read, close Issue #9 as completed and record MF-9 as `closed`.
``````

## Chronological Action Summary

| # | Action | Read / write | Target | Outcome and evidence |
| ---: | --- | --- | --- | --- |
| 1 | Read repository and GitHub baseline | Read | local worktrees, Issue #9, PR #4–#8 | Confirmed local `main` preservation boundary and five open unmerged snapshot PRs. |
| 2 | Create isolated MF-9 worktree and safety ref | Write | task branch and `refs/codex/safety/...` | Clean `origin/main` worktree established; an interrupted 12 KB initialization residue was moved to Trash and the worktree was rebuilt with LFS smudge disabled. |
| 3 | Capture MF-9 request and plan | Write | `docs/changes/9-reference-dependency-decision` | Complete request and Change-specific effective Plan saved before GitHub writes. |
| 4 | Update Active remediation scope | Write and re-read | GitHub Issue #2 | MF-2 now contains exact Active paths, versions, exclusions, stacked-PR boundary, and Ready-only endpoint. |
| 5 | Record the approved decision | Write and re-read | GitHub Issue #9 | Skip/Adopt matrix, rationale, MF-2 link, and closure contract persisted. |
| 6 | Comment on and close snapshot PRs | Write | GitHub PR #4–#8 | Posted one tailored comment per PR and closed each without a merge operation. |
| 7 | Verify external state | Read | GitHub PR #4–#8 and their comments | All five are closed with `merged: false`, `merged_at: null`; authored comment IDs were re-read. |
| 8 | Run local validation | Read | MCL, Node, Python, platform, Markdown and Git diff | 37 Node tests, seven Python tests, strict Completion, platform, link, and whitespace checks passed. |
| 9 | Publish the pre-closure archive | Write and re-read | commit `cc224f63`, branch, and PR #19 | Branch was pushed; PR file list contained only 13 MF-9 documentation files. |
| 10 | Validate the hosted Head | Read | GitHub Actions run `31334992146` | Governance/Completion, CodeQL, dependency review, and `required / gate` passed; product jobs were correctly skipped. |
| 11 | Perform separated author self-review | Read | MF-9 Spec and staged/hosted diff | Spec compliance and code quality both passed; no independent non-author review is claimed. |
| 12 | Finalize merge-effective closure | Write | MF-9 sources and deterministic generated artifacts | The final record is marked closed and becomes repository fact when PR #19 is merged. |

## Agent Reply Summary

The Agent reported the verified baseline, isolated-worktree recovery, Manifest 1.1 capture, successful unmerged closure of PR #4–#8, and green local/hosted governance Gates. MF-9 is closed by merging this final archive; the exact merge and Issue state are delivered after re-read.

## Files and External Effects

- Created only MF-9 authored governance sources in the isolated worktree.
- Created the local safety ref without moving local `main`.
- Moved the task-created 12 KB failed initialization residue to `/Users/shuangsu/.Trash/Metaflow-mf9-decision-incomplete-20260810`.
- Updated Issue #2 and Issue #9.
- Added comments `5233740405`, `5233740551`, `5233740697`, `5233740869`, and `5233741037` to PR #4–#8 respectively.
- Closed PR #4–#8 without merging them.
- No product change, snapshot change, dependency change, release, Preview, or deployment occurred.
- Pushed commit `cc224f63ea43c1f57756097d45401a2ce7b7c5aa` and created draft PR #19 against `main`.

## Validation

- Worktree HEAD equals `origin/main@91167cc3e2c2f8ceb80cbb07644aeb927b5320c9`.
- Local `main` safety ref equals `47ffd86e9635d7d797628a214df817f7ca60b55c`.
- Predecessor plan SHA-256 remains `37f45424cc233af72801e1d91053d4581d1bbcdfb73627612d6f28f018af85a3`.
- GitHub re-read returned `state: closed`, `merged: false`, and `merged_at: null` for PR #4–#8.
- MF-2 and MF-9 body content was re-read after update.
- 37 Node tests and seven Python tests passed.
- Strict MCL generation/check, platform validation, Markdown links, secret/hygiene checks, and whitespace checks passed locally and in hosted governance job `93299321593`.
- CodeQL job `93299321623`, dependency review job `93299321629`, and `required / gate` job `93299509483` passed in run `31334992146`.
- Spec-compliance self-review and code-quality self-review passed; all changed files belong to MF-9 documentation.

## Failures, Retries, and Skipped Checks

- The first worktree checkout attempted to hydrate the large LFS tree and stopped while initializing. Its exact task-created residue was moved to Trash, and a clean checkout succeeded with LFS smudge disabled.
- The first hosted run validated the pre-closure record. A final run must validate this closure-only generated update before merge.

## Plan Deviations and Approval

Existing Issue #2 is reused instead of creating a duplicate Active dependency issue. This preserves the approved ownership and does not change scope.

## Remaining Work and Continuation Conditions

Delivery operations remaining after this immutable record is generated are: push the closure commit, validate its exact Head, mark PR #19 Ready, squash-merge, re-read the merge and `main`, and close/re-read Issue #9. They do not expand implementation scope and are recorded in the Issue and final response.

## Final Delivery

- Record path: `docs/changes/9-reference-dependency-decision/completion/task-records/MF-9-T01.md`
- Dossier path: `docs/changes/9-reference-dependency-decision/completion/dossier.md`
- SHA-256: recorded in `completion/manifest.json` after final deterministic generation
- PR / commit / release / deploy: PR #19; no release or deploy
